let SHOW_NUMBERS=!1;const ITERATIONS=500,D=100,R=50,FRAME_RATE=4;let STROKE_COLOR="blue";const STROKE_WEIGHT=2,TEXT_SIZE=10,OFFSET=20;let multMin=10,multMax=30,countMin=10,countMax=30;var gui;function setup(){createCanvas(120*(multMax-multMin+1)+20,120*(countMax-countMin+1)+20),frameRate(4),stroke(STROKE_COLOR),strokeWeight(2),noFill(),textSize(10),noLoop()}function draw(){background("black");let y=70;for(let pn=countMin;pn<=countMax;pn++){STROKE_COLOR=getRndColor();let x=70;for(let mp=multMin;mp<=multMax;mp++)stroke(STROKE_COLOR),points=getAnchorPointCoordinates(x,y,R,pn),drawScene(points,x,y,R,SHOW_NUMBERS),push(),noStroke(),fill("white"),text(`(${pn},${mp})`,x-10,y-50-6),pop(),drawLines(points,mp,pn),x+=120;y+=120}}